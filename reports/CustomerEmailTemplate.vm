Hello $parameters.OPENREPORTS_USER_NAME,

This email is generated by the following report:

Name:         $report.name
Description:  $report.description

You have selected the following Customer(s):

#foreach ($result in $results)
$result.name  
$result.address
$!result.city , $!result.state $!result.postalcode
$!result.country
#end

